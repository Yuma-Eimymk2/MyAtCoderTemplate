#include <bits/stdc++.h>
using namespace std;
using i64 = long long;
using ui64 = unsigned long long;

#define rep(i, a, n) for(i64 i = a;i < n;i++)   //昇順
#define rrep(i, a, n) for(i64 i = a;i >= n;i--)   //降順
#define all(v) (v).begin(), (v).end()

int main() {
    
}   